projection implementation in class ZBP_C_EI_API_URL000 unique;
strict ( 2 );
use draft;
define behavior for ZC_EI_API_URL000 alias ZcEiApiUrl000
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}