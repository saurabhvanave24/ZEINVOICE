projection implementation in class ZBP_C_EI_DISCOUNT unique;
strict ( 2 );
use draft;
define behavior for ZC_EI_DISCOUNT alias ZcEiDiscount
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}